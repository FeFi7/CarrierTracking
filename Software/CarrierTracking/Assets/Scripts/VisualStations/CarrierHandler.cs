using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarrierHandler : MonoBehaviour
{
    public GameObject sampleCarrier;
    public GameObject sampleArea;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static void PositionRelativeTo(GameObject carrier, GameObject area, int percentX, int percentY)
    {

        
        //Vector3 location = new Vector3(area., transform.position.y, GetMouseWorldPos().z + mOffset.z)

        //carrier.transform.position = ;
    }
}
